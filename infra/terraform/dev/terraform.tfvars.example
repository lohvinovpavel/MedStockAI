# Copy to terraform.tfvars and fill in. Do not commit the real file —
# gemini_api_key belongs in TF_VAR_gemini_api_key instead, never here.
project_id         = "medstockai-dev"
letsencrypt_email  = "you@example.com"
dev_members = [
  # "teammate1@example.com",
  # "teammate2@example.com",
]
